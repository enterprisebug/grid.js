# grid.js

Draws a grid in your html5 canvas

 * FeatureList (not yet)

## Usage

```javascript
var opts = {
	distance : 20,
	lineWidth : 1.5,
	color : "green",
	caption : true
};
new Grid(opts).draw(context);
```

